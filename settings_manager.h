#pragma once

#include <wx/wx.h>
#include <map>
#include <string>
#include <vector>
#include "i_settings_observer.h"

/**
 * مدير الإعدادات (نمط Singleton)
 * يتعامل مع تخزين واسترجاع إعدادات المتصفح
 */
